// Uso de puntos y comas
let a, b, c; // En esta linea estamos declarando 3 variables
a = 5;     // se asigna el valor 5 a la variable a
b = 6;     // se asigna el valor 5 a la variable b
c = a + b; // asignamos el valor de sumar a + b a la variable c

// mientras usemos ; podemos poner varias declaraciones en una misma linea
a = 5; b = 6; c = a + b;
// el ejemplo anterior es valido pero por buenas practicas de programacion//  debe ser organizado y legible para otros.

// espacios en blanco
let persona = "carlos"; 
let person="carlos";
// alt + shift + f indenta el codigo por nosotros,haciendolo legible
// es dar formato al documento con el mous
// Bloques de codigo
function bloque() {
    let test1 = 0;
    let test2;

    if(condition) {
        let test3;
    }
}